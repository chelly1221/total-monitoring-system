# 전체 점검 및 수정 내역 — 2026-09-08

## 수정 사항

- **알람·수집:** 연속 수신 확인을 건너뛰던 임계값 판정 수정. 정상 복귀 확인 중에는 기존 경보와 원인을 유지. 문자형 UPS 상태와 알람 비활성화 판정을 설정 화면·수집기·음원 재생에서 통일. 공유 포트의 빈 파서 결과가 다른 장비의 수신 시각을 갱신하지 않도록 수정.
- **실시간 동기화:** 장비 상태·알람 해제·설정 변경·뇌전 확인 알림이 다른 창에도 전달되도록 수정. 연결 초기화 시 전체 상태를 갱신하며, React StrictMode에서 이전 소켓 종료 이벤트가 새 연결을 지우는 문제 수정.
- **장비 설정:** 포트 범위·정수 여부·설정 형식·항목 중복·커스텀 코드 검증 보강. 장비와 측정 항목을 같은 트랜잭션에서 저장. 삭제된 항목과 남아 있던 임계값 정리. 사이렌 수정 요청도 등록과 동일하게 검증.
- **뇌전 감시:** 새 낙뢰 발생 시 점검 체크 무효화. 설정을 빠르게 전환해도 피드 요청은 분당 1회 이내로 제한. 잘못된 거리·미래 시각 제외. 오래된 점검 상태의 저장·확인 차단, 저장 실패 표시, 다중 창 동기화, 폴링과 점검 저장 간 충돌 보완.
- **데이터·실행:** 잘못된 백업을 쓰기 전에 검증하고 복원·초기화 후 상태 전파. Windows에서 새 SQLite 파일 초기화 실패 수정. DB 동시 시작 시 잠금 재시도. 업로드 음원을 앱 데이터 폴더에 저장하도록 변경하고 실제 파일 형식 검사 보강.
- **프론트엔드:** 조건부 훅 호출, 렌더링 중 ref 접근, 임의 애니메이션 값으로 인한 화면 불일치 수정. 차트 갱신을 타이머로 분리. 정적 JS/CSS의 immutable 캐시 복구.
- **파서·빌드:** 커스텀 파서 반환값의 getter·Proxy 처리도 실행 제한 시간 안에서 검사. 무한대 수치 차단. 중복 프론트엔드 빌드 제거. 개발 DB가 배포용 Prisma 폴더에 복사되지 않도록 변경. 생성 파일과 외부 배포 라이브러리는 린트 대상에서 제외.
- **설치 패키지:** 음원 경로 추적으로 이전 Tauri 빌드 폴더가 중첩 포함되는 문제 수정. Next.js 실행에 필요한 파일만 복사하고 DB·환경 파일은 제외. Windows에서도 개발 경로를 가리키는 심볼릭 링크 대신 실제 의존성 파일을 복사.
- **의존성:** Next.js와 ESLint 설정을 16.3.4로 업데이트하고, 호환 범위의 취약 의존성 갱신. `tsx`의 `esbuild`는 수정 버전 0.28.2로 override.

## 검증 결과

| 검사 | 결과 |
| --- | --- |
| `npm test` | 20개 통과 |
| `npm run lint -- --max-warnings=0` | 오류·경고 0 |
| TypeScript | 통과, 배포 빌드에서도 재검증 |
| `npm audit` | 취약점 0 |
| `npm run tauri:build-frontend` | Next.js standalone·수집기 번들·배포 리소스 생성 성공 |
| `npm run tauri:build` | Windows x64 release EXE·NSIS 설치 파일 생성 성공 |
| `cargo check --manifest-path src-tauri/Cargo.toml` | 통과 |
| 격리된 standalone 서버 | 화면·API 16개 정상 응답, 정적 파일 캐시 및 잘못된 날짜·포트 요청 검증 통과 |
| 최종 배포 리소스 | DB 초기화 성공, 개발 DB·환경 파일·중첩 빌드 폴더·심볼릭 링크 없음 |

테스트는 임시 DB·별도 포트·모의 낙뢰 피드를 사용했다. 실제 사이렌·wing15 확인 기록은 작동시키지 않았다. 화면 검증은 서버 렌더링 응답 기준이며, 실제 장비 연동 및 데스크톱 전체 조작 검증은 포함하지 않았다.

사용자 요청에 따라 기존 앱·서버·수집기를 종료하고 최종 EXE와 배포 리소스를 빌드했다. 프로그램은 종료 상태로 유지했다. 운영 DB는 변경하지 않았으며, 기존 배포 리소스는 Git 제외 경로인 `.review/resources-before-native-build/`에 보관했다.

- 실행파일: `src-tauri/target/release/tms-portable.exe` (같은 폴더의 `resources/` 필요)
- 설치파일: `src-tauri/target/release/bundle/nsis/TMS Portable_0.1.0_x64-setup.exe`

생성된 실행파일·설치파일·리소스는 Git 제외 대상이며, 소스와 점검 문서를 커밋한다.

보안 수정 참고: [Next.js 보안 공지](https://github.com/advisories/GHSA-ggv3-7p47-pfv8), [ws 보안 공지](https://github.com/advisories/GHSA-96hv-2xvq-fx4p), [esbuild 보안 공지](https://github.com/advisories/GHSA-g7r4-m6w7-qqqr).
